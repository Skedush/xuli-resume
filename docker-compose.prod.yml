services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_REVISION: ${BUILD_REVISION:-unknown}
    ports:
      - "8888:80"
    restart: unless-stopped
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
